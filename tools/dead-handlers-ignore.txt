# 未挂载 handler 豁免清单
#
# 格式：<HandleName|RegisterName>  # 理由（必填，工具会校验）
# 这里只放**已核实**的：要么等价实现已在别处挂载，要么明确是上游历史包袱（不改共享文件）。
# 未核实的存量不要放这里 —— 那些留在 tools/dead-handlers-baseline.txt（棘轮基线）。

# 2026-09-26 核实（更正）：它不是死代码，而是**测试 seam** —— `internal/handlers/dashboard/`
# 下 12 个测试文件、33 处调用通过 setupTestRouter → RegisterRoutes 建路由表（package dashboard
# 内部，无法引用生产侧的 handlers.SetupDashboardRoutes，否则 import 循环）。
# 生产侧由 router.go 的 SetupDashboardRoutes 显式注册同一批 handler。
# 实测两侧路径集合：生产 76 条 / 测试 46 条，**"只在测试里注册"为 0 条** → 今日没有假绿；
# 但"两张表各写一遍"存在漂移风险（往 routes.go 加一条生产没有的路径，测试会在假路线上变绿）。
# 该摩擦已作为架构候选登记（见 architecture review），在收敛之前有意保留本函数。
RegisterRoutes  # dashboard 测试 seam（12 文件 33 处调用）；与生产路由表的漂移风险见架构候选
